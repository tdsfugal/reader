{
  "name": "reader",
  "private": true,
  "workspaces": [
    "packages/reader-app",
    "packages/reader-gql"
  ],
  "scripts": {
    "test": "jest --watch packages"
  },
  "devDependencies": {
    "jest": "^27.4.4",
    "lerna": "^4.0.0",
    "prettier": "^2.4.1"
  }
}
